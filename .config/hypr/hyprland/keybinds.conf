# These absolutely need to be on top, or they won't work consistently
bind = Super, Super_L, exec, qs ipc call TEST_ALIVE || pkill fuzzel || fuzzel # [hidden] Launcher fallback
binditn = Super, catchall, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:272, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:273, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:274, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:275, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:276, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse:277, global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse_up,  global, quickshell:overviewToggleReleaseInterrupt # [hidden]
bind = Super, mouse_down,global, quickshell:overviewToggleReleaseInterrupt # [hidden]

bindl = ,XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle # [hidden] 
bindle=, XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+ # [hidden]
bindle=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- # [hidden] 

#!
##! Apps: just normal apps
bind = Super, Q, exec, alacritty # Open Alacritty
bind = Super, E, exec, nautilus --new-window # Open Nautilus
bind = Super, I, exec, XDG_CURRENT_DESKTOP="gnome" gnome-control-center # Open Settings

##! Essentials for beginners
bind = Super, C, killactive, 
bind = Super, M, fullscreen # Toggle Fullscreen
bind = Super, F, togglefloating, 
bindd = Super, V, Clipboard history >> clipboard, global, quickshell:overviewClipboardToggle # Clipboard history >> clipboard
bind = Super, P, pseudo, # keep floating size of window when tiled 
bind = Super, J, togglesplit, # split other way round 

# Screenshot, Record, OCR, Color picker, Clipboard history
bind = Super+Shift, S, global, quickshell:regionScreenshot # Screen snip
bind = Super+Shift, S, exec, qs -c $qsConfig ipc call TEST_ALIVE || pidof slurp || hyprshot --freeze --clipboard-only --mode region --silent # [hidden] Screen snip (fallback)
bind = Super+Shift, A, global, quickshell:regionSearch # Google Lens
bind = Super+Shift, A, exec, qs -c $qsConfig ipc call TEST_ALIVE || pidof slurp || ~/.config/hypr/hyprland/scripts/snip_to_search.sh # [hidden] Google Lens (fallback)
# Color picker
bindd = Super+Shift, C, Color picker, exec, hyprpicker -a # Pick color (Hex) >> clipboard
# Fullscreen screenshot
bindl = ,Print,exec,grim - | wl-copy # Screenshot >> clipboard
bindln = Ctrl,Print, exec, mkdir -p $(xdg-user-dir PICTURES)/Screenshots && grim $(xdg-user-dir PICTURES)/Screenshots/Screenshot_"$(date '+%Y-%m-%d_%H.%M.%S')".png # Screenshot >> clipboard & file (file)
bindln = Ctrl,Print,exec,grim - | wl-copy # [hidden] Screenshot >> clipboard & file (clipboard)
# Recording stuff
bindl = Super+Shift, R, global, quickshell:regionRecord # Record region (no sound)
bindl = Super+Shift, R, exec, qs -c $qsConfig ipc call TEST_ALIVE || ~/.config/quickshell/$qsConfig/scripts/videos/record.sh # [hidden] Record region (no sound) (fallback)
bindl = Super+Alt, R, global, quickshell:regionRecord # [hidden] Record region (no sound)
bindl = Super+Alt, R, exec, qs -c $qsConfig ipc call TEST_ALIVE || ~/.config/quickshell/$qsConfig/scripts/videos/record.sh # [hidden] Record region (no sound) (fallback)
bindl = Ctrl+Alt, R, exec, ~/.config/quickshell/$qsConfig/scripts/videos/record.sh --fullscreen # [hidden] Record screen (no sound)
bindl = Super+Shift+Alt, R, exec, ~/.config/quickshell/$qsConfig/scripts/videos/record.sh --fullscreen --sound # Record screen (with sound)

#Lock screen
bind = Super, L, exec, loginctl lock-session

#!
##! Widgets
#bind = ControlSuper, C, exec, agsv1 run-js 'cycleMode();' # Cycle focus modes
bind = Ctrl+Super, R, exec, killall qs; qs -c $qsConfig & # Restart widgets
bindid = Super, Super_L, Toggle overview, global, quickshell:overviewToggleRelease # Toggle overview/launcher
bindd = Super, H, Toggle cheatsheet, global, quickshell:cheatsheetToggle # Toggle cheatsheet
bindd = Super, A, Open Settings & Notification window, global, quickshell:sidebarLeftToggle # Open AI and Tools Window
bindd = Super, D, Open Settings & Notification window, global, quickshell:sidebarRightToggle # Open Settings & Notification window
bindd = Super, N, Toggle media controls, global, quickshell:mediaControlsToggle # Show playing music player
bindd = Super, K, Toggle on-screen keyboard, global, quickshell:oskToggle # Show On-Screen Keyboard
#bind = Super, Minus, exec, agsv1 run-js 'openColorScheme.value = true; Utils.timeout(2000, () => openColorScheme.value = false);' # Show color settings
bindd = Ctrl+Alt, Delete, Toggle session menu, global, quickshell:sessionToggle # Session screen
bindle=, XF86MonBrightnessUp, exec, qs -c $qsConfig ipc call brightness increment || brightnessctl s 5%+ # [hidden]
bindle=, XF86MonBrightnessDown, exec, qs -c $qsConfig ipc call brightness decrement || brightnessctl s 5%- # [hidden]

##! Media
bindl= ,XF86AudioPrev, exec, playerctl previous || playerctl position `bc <<< "0"` # Play previous soung
bindl= ,XF86AudioNext, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"` # Play next song
bind = ,XF86AudioPlay, exec, playerctl play-pause # Play / Pause

# ##################################### Plugins #########################################
# Testing
# bind = SuperAlt, f12, exec, notify-send "Hyprland version: $(hyprctl version | head -2 | tail -1 | cut -f2 -d ' ')" "owo" -a 'Hyprland keybind'
bind = SuperAlt, f12, exec, notify-send "Millis since epoch" "$(date +%s%N | cut -b1-13)" -a 'Hyprland keybind' # [hidden]
bind = SuperAlt, Equal, exec, notify-send "Urgent notification" "Ah hell no" -u critical -a 'Hyprland keybind' # [hidden]

#!
##! Workspace navigation
## Swap windows
bind = AltShift, h, movewindow, l # Swap window to left
bind = AltShift, l, movewindow, r # Swap window to right 
bind = AltShift, k, movewindow, u # Swap window to top
bind = AltShift, j, movewindow, d # Swap window to down


# Move focus
bind = Alt, h, movefocus, l # Focus window to left
bind = Alt, l, movefocus, r # Focus window to right
bind = Alt, k, movefocus, u # Focus window to top
bind = Alt, j, movefocus, d # Focus window to down
bind = Alt, BracketLeft, movefocus, l # [hidden]
bind = Alt, BracketRight, movefocus, r # [hidden]

# Window split ratio
binde = Super, Comma, splitratio, -0.1
binde = Super, Period, splitratio, 0.1

# Switching
#/# bind = Super, Hash,, # Focus workspace # (1, 2, 3, 4, ...) under mouse
bind = SUPER, 1, focusWorkspaceOnCurrentMonitor, 1 # [hidden]
bind = SUPER, 2, focusWorkspaceOnCurrentMonitor, 2 # [hidden]
bind = SUPER, 3, focusWorkspaceOnCurrentMonitor, 3 # [hidden]
bind = SUPER, 4, focusWorkspaceOnCurrentMonitor, 4 # [hidden]
bind = SUPER, 5, focusWorkspaceOnCurrentMonitor, 5 # [hidden]
bind = SUPER, 6, focusWorkspaceOnCurrentMonitor, 6 # [hidden]
bind = SUPER, 7, focusWorkspaceOnCurrentMonitor, 7 # [hidden]
bind = SUPER, 8, focusWorkspaceOnCurrentMonitor, 8 # [hidden]
bind = SUPER, 9, focusWorkspaceOnCurrentMonitor, 9 # [hidden]
bind = SUPER, 0, focusWorkspaceOnCurrentMonitor, 10 # [hidden]
bind = Super, S, togglespecialworkspace,
bind = Alt, Tab, cyclenext # Cycle windows on monitor
bind = Alt, Tab, bringactivetotop, # [hidden]

# Move active window to a workspace with mainMod + SHIFT + [0-9]
#/# bind = Super+Alt, Hash,, # Window: move to workspace # (1, 2, 3, 4, ...)
bind = SUPER SHIFT, 1, movetoworkspace, 1 # [hidden]
bind = SUPER SHIFT, 2, movetoworkspace, 2 # [hidden]
bind = SUPER SHIFT, 3, movetoworkspace, 3 # [hidden]
bind = SUPER SHIFT, 4, movetoworkspace, 4 # [hidden]
bind = SUPER SHIFT, 5, movetoworkspace, 5 # [hidden]
bind = SUPER SHIFT, 6, movetoworkspace, 6 # [hidden]
bind = SUPER SHIFT, 7, movetoworkspace, 7 # [hidden]
bind = SUPER SHIFT, 8, movetoworkspace, 8 # [hidden]
bind = SUPER SHIFT, 9, movetoworkspace, 9 # [hidden]
bind = SUPER SHIFT, 0, movetoworkspace, 10 # [hidden]
bind = ControlShiftSuper, Up, movetoworkspacesilent, special # [hidden]
bind = SuperAlt, S, movetoworkspacesilent, special # [hidden]

#/# bind = Super, Scroll ↑/↓,, # Workspace: focus left/right+
bind = Super, mouse_up, workspace, +1 # [hidden]
bind = Super, mouse_down, workspace, -1 # [hidden]

# Move/resize windows with Super + LMB/RMB and dragging
bindm = Super, mouse:272, movewindow
bindm = Super, mouse:273, resizewindow
# bindm = Super, mouse:274, movewindow
bindm = Super, Z, movewindow # [hidden]
bind = ControlSuper, Backslash, resizeactive, exact 640 480 # [hidden]
 
